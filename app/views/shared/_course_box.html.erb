<section>
	<div class="coursebox">
		<% @courses.each do |course| %>
		<div class="course">
			<%= link_to image_tag(course.cover_image_url(:box)), course_path(course) %> <br />
			<h4><%= course.title %><br /></h4>
		</div>
		<% end %>
	</div>
</section>