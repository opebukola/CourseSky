var qid = <%=@lesson_item.id.to_json %>;
var response = $('#hint-box_' + qid);
response.addClass("response").html("Correct!")
<% if current_user && current_user.completed_lesson?(@lesson) %>
	window.location.href="<%=@lesson.id%>/share"
<% end %>
