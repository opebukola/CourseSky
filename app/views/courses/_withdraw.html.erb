<%= simple_form_for(current_user.enrollments.find_by_enrolled_course_id(@course), html: {method: :delete}, remote:true) do |f| %>
	<%= f.button :submit, "You are enrolled in this course. Click to leave course" %>
<% end %>