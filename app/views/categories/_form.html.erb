<%= simple_form_for @category do |f| %>
	<%= f.input :name %>
	<%= f.association :parent, collection: Category.all, label: 'Select Parent Category' %>
	<%= f.button :submit, "Add Category" %>
<% end %>