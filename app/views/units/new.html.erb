<%= simple_form_for @unit do |f| %>
	<%= f.input :course_id, as: :hidden, input_html: {value: params[:course_id]} %>
  <%= f.input :title, label: 'Give the unit a title' %>
  <%= f.button :submit, "Save Unit" %>
<% end %>
