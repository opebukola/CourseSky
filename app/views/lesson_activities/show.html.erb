<%= provide(:title, @lesson.title) %>
<%= render 'shared/course_nav' %>
<%= render 'shared/activity_nav' %>


<% if @activity.activity_type == 'Concept' %>
	<%= render 'show_concept' %>
<% elsif @activity.activity_type == 'Question'%>
	<%= render 'show_question' %>
<% else %>
<% end %>


